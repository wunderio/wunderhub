resources:
  'entity:node':
    GET:
      supported_formats:
        - hal_json
      supported_auth:
        - basic_auth
  menutree:
    GET:
      supported_formats:
        - hal_json
        - json
      supported_auth:
        - basic_auth
